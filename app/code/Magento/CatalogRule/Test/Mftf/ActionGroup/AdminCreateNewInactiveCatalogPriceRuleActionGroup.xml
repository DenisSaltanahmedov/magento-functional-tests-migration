<?xml version="1.0" encoding="UTF-8"?>
<!--
 /**
  * Copyright © Magento, Inc. All rights reserved.
  * See COPYING.txt for license details.
  */
-->

<actionGroups xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
              xsi:noNamespaceSchemaLocation="urn:magento:mftf:Test/etc/actionGroupSchema.xsd">
    <actionGroup name="AdminCreateNewInactiveCatalogPriceRuleActionGroup">
        <annotations>
            <description>Goes to the Admin Catalog Price Rule creation page. Fills in the provided Catalog Rule details. Selects the provided Customer Group.</description>
        </annotations>
        <arguments>
            <argument name="catalogRule" defaultValue="_defaultCatalogRule"/>
        </arguments>

        <amOnPage url="{{AdminNewCatalogPriceRulePage.url}}" stepKey="openNewCatalogPriceRulePage"/>
        <waitForPageLoad stepKey="waitForPageToLoad"/>
        <fillField stepKey="fillName" selector="{{AdminNewCatalogPriceRule.ruleName}}" userInput="InactiveCatalogRule"/>
        <fillField stepKey="fillDescription" selector="{{AdminNewCatalogPriceRule.description}}" userInput="{{catalogRule.description}}"/>
        <selectOption stepKey="selectWebSite" selector="{{AdminNewCatalogPriceRule.websites}}" userInput="{{catalogRule.website_ids[0]}}"/>
        <selectOption selector="{{AdminNewCatalogPriceRule.customerGroups}}" userInput="General" stepKey="selectCustomerGroup"/>
        <scrollTo selector="{{AdminNewCatalogPriceRule.actionsTab}}" stepKey="scrollToActionTab"/>
        <click stepKey="openActionDropdown" selector="{{AdminNewCatalogPriceRule.actionsTab}}"/>
        <selectOption stepKey="discountType" selector="{{AdminNewCatalogPriceRuleActions.apply}}" userInput="by_fixed"/>
        <fillField stepKey="fillDiscountValue" selector="{{AdminNewCatalogPriceRuleActions.discountAmount}}" userInput="10"/>
        <selectOption stepKey="discardSubsequentRules" selector="{{AdminNewCatalogPriceRuleActions.disregardRules}}" userInput="Yes"/>
        <wait time="5" stepKey="waitForPageLoad2"/>
        <scrollToTopOfPage stepKey="scrollToTop"/>
        <click selector="{{AdminNewCatalogPriceRule.save}}" stepKey="saveTheCatalogRule"/>
        <see selector="{{AdminCategoryMessagesSection.SuccessMessage}}" userInput="You saved the rule." stepKey="assertSuccess"/>
        <see selector="{{AdminMessagesSection.noticeMessage}}" userInput='We found updated rules that are not applied. Please click "Apply Rules" to update your catalog.' stepKey="noticeMessage"/>
    </actionGroup>
    <actionGroup name="SeeInactiveCatalogPriceRuleInGridActionGroup">
        <annotations>
            <description>Check if the Catalog rule in in grid</description>
        </annotations>
        <see selector="{{AdminCatalogPriceRuleGrid.firstRow}}" userInput="InactiveCatalogRule" stepKey="seeCatalogRuleName"/>
    </actionGroup>
</actionGroups>
